#include <stdio.h>


/* This is one of my favorite Disney song parodies where the lyrics are printed in actual C code.
It is of course a parody of Under The Sea from The Little Mermaid. */

int main(void) {

printf("Press enter to print the lyrics:\n");
getchar();

printf("Ariel, listen to me. OO languages, it's a mess. Programming in C is better than anything they've got over there!\n");
getchar();

printf("The syntax would seem much sweeter where objects and subtypes play\n");
getchar();

printf("But frills like inheritance will only get in the way\n");
getchar();

printf("Admire C's simple landscape, efficiently dangerous\n");
getchar();

printf("No templates or fancy pitfalls like Java and C++\n");
getchar();

printf("Program in C\n");
getchar();

printf("Program in C\n");
getchar();

printf("Pointers, assembly, manage your memory with malloc() and free()\n");
getchar();


printf("Don't sink your app with runtime bloat, software in C will stay afloat\n");
getchar();

printf("Do what you want there, close to the hardware, program in C\n");
getchar();

printf("With C all the coders happy as it's all procedural\n");
getchar();

printf("The dudes with .NET ain't happy; they forgot to call Stream.Dispose()\n");
getchar();

printf("They like to think they so lucky the runtime will clean their act\n");
getchar();

printf("But when you abuse the memory, you're bottlenecked, that's a fact! Oh, well!\n");
getchar();

printf("Program in C\n");
getchar();

printf("Program in C\n");
getchar();

printf("Nobody's faster, -O2's the master indubitably\n");
getchar();

printf("We what the coders like to roast but they don't have a lot to boast\n");
getchar();

printf("Pure satisfaction, little abstraction, program in C\n");
getchar();

printf("Program in C (program in C)\n");
getchar();

printf("Program in C (program in C)\n");
getchar();

printf("Neither exceptions nor your contemption can bother me (can bother me)\n");
getchar();

printf("Windows and Linux demonstrate portable code is running great\n");
getchar();

printf("There'e implementations for all occasions, program in C");
getchar();


return 0;
}
